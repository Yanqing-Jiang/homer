# ====================================================================
# HOMER ENVIRONMENT — copy to .env and fill in.
#
# At minimum you need TELEGRAM_BOT_TOKEN + ALLOWED_CHAT_ID + one of
# the model providers (or a Claude Code CLI already authed). Everything
# else is opt-in.
# ====================================================================

# --- Owner identity (used in prompts, scraping, telephony) ------------
OWNER_DISPLAY_NAME=Your Name
OWNER_EMAIL=you@example.com
OWNER_PHONE=+15555550100
LINKEDIN_HANDLE=your-linkedin-handle
MEDIUM_PROFILE_URL=https://medium.com/@your-handle

# --- Telegram bot -----------------------------------------------------
# Both required at boot — config validation rejects empty/non-numeric values.
# Get the bot token from @BotFather; chat ID from @userinfobot (positive integer).
TELEGRAM_BOT_TOKEN=your_telegram_bot_token
ALLOWED_CHAT_ID=123456789

# --- Session & logging -----------------------------------------------
SESSION_TTL_HOURS=4
LOG_LEVEL=info

# --- Model providers --------------------------------------------------
OPENAI_API_KEY=your_openai_api_key
MOONSHOT_API_KEY=your_moonshot_api_key
NVIDIA_NIM_API_KEY=your_nvidia_nim_api_key
GEMINI_API_KEY=your_gemini_api_key
GEMINI_API_KEY_Primary=your_gemini_primary_key

# --- ElevenLabs (voice + telephony) ----------------------------------
ELEVEN_LABS_API_KEY=your_elevenlabs_api_key
ELEVENLABS_AGENT_ID=your_elevenlabs_agent_id
ELEVENLABS_PHONE_NUMBER_ID=your_elevenlabs_phone_number_id
HOMER_PHONE=+15555550101

# --- Azure Blob Storage (optional media storage) ---------------------
AZURE_STORAGE_CONNECTION_STRING=your_azure_connection_string
AZURE_STORAGE_CONTAINER=homer-data

# --- Telephony webhook server ----------------------------------------
TELEPHONY_ENABLED=true
TELEPHONY_HOST=127.0.0.1
TELEPHONY_PORT=3000
TELEPHONY_PUBLIC_URL=https://your-tunnel.example.com   # public origin Twilio uses for signature validation
# HOMER_API_URL is kept as a backward-compatible alias for TELEPHONY_PUBLIC_URL.
# HOMER_API_URL=https://your-tunnel.example.com

# --- Twilio (inbound SMS + outbound notifications) -------------------
TWILIO_ACCOUNT_SID=your_twilio_account_sid
TWILIO_AUTH_TOKEN=your_twilio_auth_token
TWILIO_PHONE_NUMBER=+15555550101

# --- ElevenLabs webhook verification ---------------------------------
ELEVENLABS_WEBHOOK_SECRET=your_elevenlabs_webhook_secret
